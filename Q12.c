// aloha[2] = value;  // Valido, o array aloha de indice 2 recebe o valor da variavel value.
// scanf("%f", &aloha);  // Invalido, é necessário especificar um índice. 
// aloha = "value";  // Invalido, pois esta atribuindo uma string diretamente a um array de float.
// printf("%f", aloha); // Invalido, imprimir esse ponteiro diretamente usando o especificador de formato %f é inválido porque você está tentando imprimir um ponteiro como um float.
// coisas[4][4] = aloha[3]; // Valido, a matriz na coluna 4 linha 4 recebe o valor que está no vetor no índice 3.
// coisas[5] = aloha; // Invalido, coisas é matriz e não array, e aloha não está especificado o índice.
// pf = value; // Invalido, ponteiro não pode direcionar direto para uma variavel e não para seu endereço de memória.
// pf = aloha; // Valido, ponteiro apontando para um array.
